{
  "name": "ledgerio-ocr-backend",
  "version": "1.0.0",
  "description": "Ledgerio OCR (receipt scan) backend — runs Tesseract OCR for the mobile app",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "multer": "^2.1.1",
    "tesseract.js": "^7.0.0"
  }
}
